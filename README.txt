This project is a nestjs project.

It has 2 routes :

login : receives username and password in json form.

Example :
{
    "username" : "tamar1", 
    "password" : "12345"
}

icon:

download an icon that is used as the backround for the login 
screen in the client. receives the file path as the query string.